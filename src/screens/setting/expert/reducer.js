const initialState = {
};
const settingExpertReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};
export default settingExpertReducer;
