const initialState = {
};
const accountExpertReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};
export default accountExpertReducer;
