import { StyleSheet, Platform } from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    flexWrap: 'wrap',
  },
  boldTextStyle: {
    flexWrap: 'wrap',
    // fontFamily: '',
  },
});
export default styles;
