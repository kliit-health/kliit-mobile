const initialState = {
};
const changePasswordReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};
export default changePasswordReducer;
